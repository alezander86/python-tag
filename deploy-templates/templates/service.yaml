apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-tag.fullname" . }}
  labels:
    {{- include "python-tag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-tag.selectorLabels" . | nindent 4 }}
