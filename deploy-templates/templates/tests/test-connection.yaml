apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-tag.fullname" . }}-test-connection"
  labels:
    {{- include "python-tag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python-tag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
